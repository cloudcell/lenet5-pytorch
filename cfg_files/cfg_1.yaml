DEVICE: "cuda"

PATHS:
    DATASET: "/Users/maorshutman/data/FashionMNIST"

TRAIN:
    BATCH_SIZE: 8
    LR: 1.0e-2
    MOMENTUM: 0.0
    EPOCHS: 10000
    BATCHNORM: False
    DROPOUT: False
    LOG_INTERVAL: 30
    LOG_DIR: "./logs/runs_2"

TEST:
    BATCH_SIZE: 64
