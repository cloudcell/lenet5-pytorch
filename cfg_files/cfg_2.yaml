DEVICE: "cuda"

PATHS:
    #DATASET: "/raid/algo/SOCISP_SLOW/ADAS/Courses/tau_dl/data/FashionMNIST"
    DATASET: "/Users/maorshutman/data/FashionMNIST"
    CHECKPOINTS_PATH: "./checkpoints"

MODEL:
    ORIG_C3: False
    ORIG_SUBSAMPLE: False
    ACTIVATION: 'relu'

    DROPOUT: 0.0
    BATCHNORM: True

TRAIN:
    BATCH_SIZE: 256
    EPOCHS: 20
    STEPS_PER_EPOCH: -1

    LR: 1.0
    MOMENTUM: 0.9
    WEIGHT_DECAY: 0.0

    LOG_INTERVAL: 10
    LOG_DIR: "./logs/runs_42"

TEST:
    BATCH_SIZE: 256
    STEPS: -1
